package croc.ex_3_2;

public class Check_chess {
  public Check_chess(String alpha, String alpha_next) throws IllegalArgumentException {
    throw new IllegalArgumentException("конь так не ходит:" + alpha + " -> " + alpha_next);
  }
